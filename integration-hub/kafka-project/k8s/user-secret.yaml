# IMPORTANT: Replace placeholder values before applying.
# Create with literals (recommended) instead of committing:
#
#   kubectl -n ecommerce create secret generic user-service-secret \
#     --from-literal=DB_HOST=<postgres-host> \
#     --from-literal=DB_USER=<db-user> \
#     --from-literal=DB_PASSWORD=<db-password> \
#     --from-literal=JWT_SECRET=<long-random-string>
#
# This file is a template — DO NOT commit real credentials.
apiVersion: v1
kind: Secret
metadata:
  name: user-service-secret
  namespace: ecommerce
type: Opaque
stringData:
  DB_HOST: "REPLACE_ME"
  DB_USER: "REPLACE_ME"
  DB_PASSWORD: "REPLACE_ME"
  JWT_SECRET: "REPLACE_ME_LONG_RANDOM_STRING"
